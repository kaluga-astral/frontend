import { Meta } from '@storybook/blocks';
import ReactJson from 'react-json-view';

import { Brand } from './constants';
import { createTheme } from './baseTheme';

<Meta title="Components/Theme/Theme" />

# Theme

Используется совместно с [ThemeProvider](/story/components-theme-themeprovider--docs)() и styled.

## ThemeViewer

<ReactJson
  collapsed
  name="theme"
  style={{ width: '100%', marginBottom: '24px', marginTop: '24px' }}
  src={createTheme({
    brand: Brand.DEFAULT,
    fontsUrls: {
      bold: {
        woff: '',
        woff2: '',
      },
      light: {
        woff: '',
        woff2: '',
      },
      regular: {
        woff: '',
        woff2: '',
      },
      medium: {
        woff: '',
        woff2: '',
      },
    },
  })}
  theme="monokai"
  displayObjectSize={false}
  displayDataTypes={false}
/>

## Создание темы

```tsx
import { Brand, createTheme, styled } from '@astral/ui';

import UbuntuBoldWoff from '@astral/ui/fonts/UbuntuBold.woff';
import UbuntuBoldWoff2 from '@astral/ui/fonts/UbuntuBold.woff2';
import UbuntuLightWoff from '@astral/ui/fonts/UbuntuLight.woff';
import UbuntuLightWoff2 from '@astral/ui/fonts/UbuntuLight.woff2';
import UbuntuRegularWoff from '@astral/ui/fonts/UbuntuRegular.woff';
import UbuntuRegularWoff2 from '@astral/ui/fonts/UbuntuRegular.woff2';
import UbuntuMediumWoff from '@astral/ui/fonts/UbuntuMedium.woff';
import UbuntuMediumWoff2 from '@astral/ui/fonts/UbuntuMedium.woff2';

const fontsUrls = {
  bold: {
    woff: UbuntuBoldWoff,
    woff2: UbuntuBoldWoff2,
  },
  light: {
    woff: UbuntuLightWoff,
    woff2: UbuntuLightWoff2,
  },
  regular: {
    woff: UbuntuRegularWoff,
    woff2: UbuntuRegularWoff2,
  },
  medium: {
    woff: UbuntuMediumWoff,
    woff2: UbuntuMediumWoff2,
  },
};

const theme = createTheme({ brand: Brand.DEFAULT, fontsUrls });

const Main = styled.main`
  padding: ${({ theme }) => theme.spacing(4)};
`;

const App = () => (
  <ThemeProvider theme={theme}>
    <Main />
  </ThemeProvider>
);
```

## Brand

Тема поддерживает брендирование.

Поддерживаемые бренды:

```ts
export enum Brand {
  DEFAULT = 'DEFAULT',
  EDO = 'EDO',
  KEDO = 'KEDO',
  AO5 = 'AO5',
  OFD = 'OFD',
  SIGN = 'SIGN',
  LKP = 'LKP',
  POA = 'POA',
  RSS = 'RSS',
  SBER = 'SBER',
  LK1C = 'LK1C',
  PLATFORM = 'PLATFORM',
}
```

## useTheme

Для доступа к `theme` внутри компонента можно использовать `useTheme`.

```tsx
import { useTheme } from '@astral/ui';

const Card = () => {
  const theme = useTheme();

  return (
    <article>
      <p>Primary: {theme.palette.primary.main}</p>
    </article>
  );
};
```
