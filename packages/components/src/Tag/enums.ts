export enum TagColors {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  ERROR = 'error',
  GREY = 'grey',
  SUCCESS = 'success',
  WARNING = 'warning',
}

export enum TagVariants {
  CONTAINED = 'contained',
  LIGHT = 'light',
  TEXT = 'text',
}

export enum TagStates {
  DEFAULT = 'default',
  HOVER = 'hover',
  ACTIVE = 'active',
}
