/**
 * Типы перехваченных ошибок
 * @enum OutdatedRelease - ошибка при обновлении приложения
 * @enum Default - все остальные ошибки при выполнения кода
 */
export enum TypeError {
  OutdatedRelease = 'outdatedRelease',
  Default = 'default',
}
