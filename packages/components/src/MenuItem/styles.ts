import { Tooltip } from '../Tooltip';
import { styled } from '../styles';

export const MenuItemTooltip = styled(Tooltip)`
  white-space: pre-line;
`;

export const Link = styled('a')`
  all: unset;
`;
