import { Meta } from '@storybook/react';

/**
 * Функция для стилизации компонентов.
 */
const meta: Meta = {
  title: 'Components/Styles/Styled',
};

export default meta;

export const Interaction = {
  parameters: {
    docs: {
      disable: true,
    },
  },
};
