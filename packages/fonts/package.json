{
  "name": "@astral/fonts",
  "version": "1.0.0",
  "main": "./src",
  "scripts": {
    "prebuild": "npx rimraf ./lib",
    "build": "npx commander copyCommonFiles && npx commander copyFonts && npx commander modifyPackageJSON",
    "publish": "yarn run build && npx commander publish"
  }
}
