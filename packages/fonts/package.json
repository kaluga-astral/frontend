{
  "name": "@astral/fonts",
  "version": "0.5.0",
  "main": "./src",
  "scripts": {
    "prebuild": "npx rimraf ./lib",
    "build": "node ../../commander copyCommonFiles && node ../../commander copyFonts && node ../../commander modifyPackageJSON",
    "publish": "yarn build && node ../../commander publish"
  }
}