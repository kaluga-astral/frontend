export enum TagColors {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  ERROR = 'error',
  GREY = 'grey',
  SUCCESS = 'success',
  WARNING = 'warning',
}

export enum TagVariants {
  CONTAINED = 'contained',
  LIGHT = 'light',
}

export enum TagSizes {
  MEDIUM = 'medium',
  SMALL = 'small',
}

export enum TagStates {
  DEFAULT = 'default',
  HOVER = 'hover',
  ACTIVE = 'active',
}
