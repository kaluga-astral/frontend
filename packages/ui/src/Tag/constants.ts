export enum TagColors {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ERROR = 'error',
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
}

export enum TagVariants {
  CONTAINED = 'contained',
  LIGHT = 'light',
}

export enum TagSizes {
  MEDIUM = 'medium',
  SMALL = 'small',
}

export enum TagStates {
  DEFAULT = 'default',
  HOVER = 'hover',
  ACTIVE = 'active',
  SELECTED = 'selected',
}
