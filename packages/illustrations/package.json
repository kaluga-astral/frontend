{
  "name": "@astral/illustrations",
  "main": "./src",
  "version": "0.31.0",
  "scripts": {
    "prebuild": "rimraf ./lib",
    "build": "commander copyCommonFiles && commander copyImages && commander modifyPackageJSON --isStaticPackage=true",
    "publish": "npm publish ./lib"
  }
}
