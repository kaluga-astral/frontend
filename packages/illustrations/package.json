{
  "name": "@astral/illustrations",
  "main": "./src",
  "version": "0.31.0",
  "scripts": {
    "svgo": "npx rimraf ./dist && svgo -q --config svgo.config.js -f src -o dist",
    "prebuild": "rimraf ./lib",
    "build": "commander copyCommonFiles && commander copyImages && commander modifyPackageJSON --isOnlyStaticPackage=true",
    "publish": "npm publish ./lib"
  },
  "dependencies": {
    "svgo": "^2.8.0"
  }
}
