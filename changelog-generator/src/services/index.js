const { FileService } = require('./fileService');
const { MarkdownService } = require('./markdownService');
const { TelegramService } = require('./telegramService');

module.exports = { FileService, MarkdownService, TelegramService };
