name: 'Chromatic'

on:
  pull_request:
    branches: [main, next]
    paths-ignore:
      - packages/**
      - .storybook/**
      - package.json
      - package-lock.json

jobs:
  chromatic-deployment:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No chromatic-deployment required"'
