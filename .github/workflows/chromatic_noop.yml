name: 'Chromatic'

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - packages/ui/**
      - packages/icons/**
      - packages/fonts/**
      - .storybook/**
      - package.json
      - package-lock.json

jobs:
  chromatic-deployment:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No chromatic-deployment required"'
